# QuikTrac Environment Example
# Copy to .env and fill values. Never commit your real .env

# Runtime
NODE_ENV=development

# Database (Postgres on Hetzner; include ?sslmode=require if needed)
DATABASE_URL=postgresql://user:password@host:5432/quiktrac?schema=public

# OpenAI
OPENAI_API_KEY=sk-...

# Ceipal ATS
CEIPAL_CLIENT_ID=
CEIPAL_CLIENT_SECRET=
CEIPAL_USERNAME=
CEIPAL_PASSWORD=
# Optional; defaults to Ceipal base if omitted
CEIPAL_BASE_URL=https://...

# Google Custom Search (for LinkedIn discovery)
GOOGLE_CSE_KEY=
GOOGLE_CSE_CX=

# AWS (Mumbai region recommended)
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# S3 for resumes and assets
S3_BUCKET=
# Optional: if using KMS for S3
S3_KMS_KEY_ID=
# SES sender identity (verified email or domain)
SES_FROM_EMAIL=notifications@yourdomain.com

# WhatsApp provider (choose one: meta | twilio)
WHATSAPP_PROVIDER=meta

# Meta WhatsApp Business (if WHATSAPP_PROVIDER=meta)
META_WHATSAPP_ACCESS_TOKEN=
META_WHATSAPP_PHONE_NUMBER_ID=
META_WHATSAPP_BUSINESS_ACCOUNT_ID=

# Twilio WhatsApp (if WHATSAPP_PROVIDER=twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+1xxxxxxxxxx

# Vapi (Voice outreach)
VAPI_API_KEY=

# Proxy (for scraping; optional)
PROXY_URL=http://user:pass@host:port
